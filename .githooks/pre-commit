#!/bin/sh
set -e

echo "Running checks before commit..."

echo "\n→ Accessibility checks"
pnpm a11y

echo "\n→ TypeScript tests"
pnpm test

echo "\n→ Rust tests"
cd src-tauri && cargo test --quiet
cd ..

echo "\nAll tests passed."
